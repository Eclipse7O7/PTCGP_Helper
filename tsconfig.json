{
  "compilerOptions": {
    // (./types.d.ts doesn't transpile)
    "types": ["./types"],
    "target": "ESNext",
    "useDefineForClassFields": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": false,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    //"noEmit": true,
    "jsx": "react-jsx",

    //"outDir": "../../dist-electron",

    
    
  },
  "include": ["src"],
  "exclude": ["src/electron"],
  //"files": [],
  "references": [
    //Gives a warning that it doesn't disable emit, but it needs to be set to
    //  true for allowImportingTsExtensions to work and for it to build properly
    //  Ended up commenting it out in both as well as allowImportingTsExtensions
    //  and seems to let it build now \_o_/
    { "path": "./tsconfig.node.json" }
  ], 
}
